<h1>社員一覧</h1>
<table class="table">
	<thead>
		<tr>
			<th>name</th>
			<th>email</th>
		</tr>
	</thead>
	<tbody>
	
		<% @users.each do|user| %>
			<tr>
				<td><%= link_to user.name, user_path(user.id) %></td>
				<td><%= user.email %></td>
			</tr>
		<% end %>

	
	</tbody>
</table>

 